'use strict';
/*
describe('Component: GardenComponent', function () {

  // load the controller's module
  beforeEach(module('heirloamApp'));

  var GardenComponent, scope;

  // Initialize the controller and a mock scope
  beforeEach(inject(function ($componentController, $rootScope) {
    scope = $rootScope.$new();
    GardenComponent = $componentController('GardenComponent', {
      $scope: scope
    });
  }));

  it('should ...', function () {
    expect(1).toEqual(1);
  });
});
*/