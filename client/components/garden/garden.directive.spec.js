'use strict';
/*
describe('Directive: garden', function () {

  // load the directive's module and view
  beforeEach(module('heirloamApp'));
  beforeEach(module('components/garden/garden.html'));

  var element, scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));

  it('should make hidden element visible', inject(function ($compile) {
    element = angular.element('<garden></garden>');
    element = $compile(element)(scope);
    scope.$apply();
    expect(element.text()).toBe('this is the garden directive');
  }));
});
*/